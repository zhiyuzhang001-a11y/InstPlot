# Install the locked runtime dependencies first, then install this local project with --no-deps.
# Regenerate the lock on Python 3.12 with:
# uv pip compile pyproject.toml --python-version 3.12 --universal --generate-hashes --output-file requirements.lock
-r requirements.lock
